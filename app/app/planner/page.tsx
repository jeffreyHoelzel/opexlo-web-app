import { PlaceholderPage } from "@/components/placeholder-page";

export default function PlannerPage() {
  return (
    <PlaceholderPage
      description="A mindful daily planning workflow for selecting realistic work, setting estimates, and choosing top priorities."
      eyebrow="Plan"
      items={["Morning plan", "Available time warning", "Top priorities"]}
      title="Planner"
    />
  );
}
