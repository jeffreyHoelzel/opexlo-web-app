import { PlaceholderPage } from "@/components/placeholder-page";

export default function NewNotePage() {
  return (
    <PlaceholderPage
      description="The note creation route will use Tiptap for lightweight rich text and store structured JSON plus plain text."
      eyebrow="Notes"
      title="New note"
    />
  );
}
