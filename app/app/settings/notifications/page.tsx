import { PlaceholderPage } from "@/components/placeholder-page";

export default function NotificationSettingsPage() {
  return (
    <PlaceholderPage
      description="Notification settings will manage task reminders, planning reminders, shutdown reminders, and email delivery preferences."
      eyebrow="Settings"
      title="Notifications"
    />
  );
}
