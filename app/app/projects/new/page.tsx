import { PlaceholderPage } from "@/components/placeholder-page";

export default function NewProjectPage() {
  return (
    <PlaceholderPage
      description="The project creation route will reuse a shared project form for name, area, description, color, and status."
      eyebrow="Projects"
      title="New project"
    />
  );
}
